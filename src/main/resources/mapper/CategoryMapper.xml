<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.lancit.domain.calendar.category.mapper.CategoryMapper">

    <select id="findAll" resultType="com.ssafy.lancit.domain.calendar.category.dto.CategoryDTO">
        SELECT
            category_id,
            email,
            owner_type,
            category_name,
            color
        FROM category
        WHERE email = #{email}
          AND owner_type = #{ownerType}
        ORDER BY category_id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="categoryId">
        INSERT INTO category (
            email,
            owner_type,
            category_name,
            color
        ) VALUES (
            #{email},
            #{ownerType},
            #{categoryName},
            #{color}
        )
    </insert>

    <select id="findByIdAndOwner" resultType="com.ssafy.lancit.domain.calendar.category.dto.CategoryDTO">
        SELECT
            category_id,
            email,
            owner_type,
            category_name,
            color
        FROM category
        WHERE category_id = #{categoryId}
          AND email = #{email}
          AND owner_type = #{ownerType}
    </select>

    <update id="update">
        UPDATE category
        <set>
            <if test="dto.categoryName != null">
                category_name = #{dto.categoryName},
            </if>
            <if test="dto.color != null">
                color = #{dto.color},
            </if>
        </set>
        WHERE category_id = #{categoryId}
          AND email = #{email}
          AND owner_type = #{ownerType}
    </update>

    <!-- OwnerCheckAspect 에서 카테고리 소유자 검증 시 사용 -->
    <select id="findOwnerEmailById" parameterType="int" resultType="string">
        SELECT email
        FROM category
        WHERE category_id = #{categoryId}
    </select>

    <select id="existsByIdAndOwner" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM category
        WHERE category_id = #{categoryId}
          AND email = #{email}
          AND owner_type = #{ownerType}
    </select>

    <delete id="deleteByIdAndOwner">
        DELETE FROM category
        WHERE category_id = #{categoryId}
          AND email = #{email}
          AND owner_type = #{ownerType}
    </delete>

    <delete id="delete">
        DELETE FROM category
        WHERE category_id = #{categoryId}
    </delete>

    <delete id="deleteByOwner">
        DELETE FROM category
        WHERE email = #{email}
          AND owner_type = #{ownerType}
    </delete>

</mapper>

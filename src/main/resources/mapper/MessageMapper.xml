<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.lancit.domain.contract.mapper.MessageMapper">

    <!-- TODO 지원: 채팅 메시지 목록 조회
         - WHERE chat_room_id = #{chatRoomId}
         - AND is_deleted = false
         - ORDER BY created_at ASC -->
    <select id="findByChatRoom" resultType="com.ssafy.lancit.domain.contract.dto.MessageDTO">

    </select>

    <!-- TODO 지원: 메시지 삽입
         - useGeneratedKeys="true" keyProperty="messageId" 유지할 것
         - created_at = NOW() 로 처리
         - is_read = false, is_deleted = false, is_updated = false 기본값 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="messageId">

    </insert>

    <!-- TODO 지원: 메시지 수정
         - null 인 필드는 업데이트 제외 → <if test="xxx != null"> 사용
         - is_updated = true (수정 시)
         - is_deleted = true (soft delete 시)
         - WHERE message_id = #{messageId} -->
    <update id="update">

    </update>

    <!-- TODO 지원: 메시지 발신자 이메일 조회
         - SELECT email FROM message WHERE message_id = #{messageId}
         - OwnerCheck (수정/삭제 시 본인 메시지인지 검증) 에서 사용 -->
    <select id="findOwnerEmail" resultType="String">

    </select>

</mapper>
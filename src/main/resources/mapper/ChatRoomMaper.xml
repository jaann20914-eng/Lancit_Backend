<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.lancit.domain.contract.mapper.ChatRoomMapper">

    <!-- TODO 지원: 채팅방 단건 조회
         - WHERE contract_id = #{contractId} -->
    <select id="findByContract" resultType="com.ssafy.lancit.domain.contract.dto.ChatRoomDTO">

    </select>

    <!-- TODO 지원: 채팅방 삽입
         - useGeneratedKeys="true" keyProperty="chatRoomId" 유지할 것 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="chatRoomId">

    </insert>

</mapper>